# Пакет-маркер. НЕ создавайте здесь отдельный Flask(__name__) — это
# приводило к тому, что CORS применялся к "призрачному" приложению,
# которое никогда не запускалось, а реально работал другой инстанс,
# созданный в run.py::create_app() без CORS.
# Вся сборка приложения (Flask(), CORS(), регистрация blueprint'ов) —
# в run.py::create_app().
 